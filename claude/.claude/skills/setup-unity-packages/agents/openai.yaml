interface:
  display_name: "Unity 6.3 Package Setup"
  short_description: "Resolve UPM and import asset packages once"
  default_prompt: "Use $setup-unity-packages to resolve and pin the required Unity 6000.3.21f1 UPM graph in Phase A, then import and configure the registered Unity asset packages in one Editor-dependent Phase B."
