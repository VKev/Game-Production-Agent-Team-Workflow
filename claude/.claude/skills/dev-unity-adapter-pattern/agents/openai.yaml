interface:
  display_name: "Unity Adapter Pattern"
  short_description: "Bridge incompatible Unity-facing interfaces"
  default_prompt: "Use $dev-unity-adapter-pattern to compare its tradeoffs with simpler Unity alternatives, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
