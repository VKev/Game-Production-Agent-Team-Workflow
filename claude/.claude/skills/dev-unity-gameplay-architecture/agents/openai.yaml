interface:
  display_name: "Unity Gameplay Architecture"
  short_description: "Select practical Unity architecture and folders"
  default_prompt: "Use $dev-unity-gameplay-architecture to analyze this Unity feature and select the simplest suitable architecture and folder structure. Compare relevant options, define responsibilities, ownership, dependencies, lifetimes, communication, file placement, assembly boundaries, GDD-backed extension seams, implementation scope, managed context refresh, and verification."
policy:
  allow_implicit_invocation: true
  products:
    - chatgpt
    - codex
    - api
    - atlas
