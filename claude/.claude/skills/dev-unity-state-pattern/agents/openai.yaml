interface:
  display_name: "Unity State Pattern"
  short_description: "Model explicit Unity behavior states and transitions"
  default_prompt: "Use $dev-unity-state-pattern to compare its tradeoffs with simpler Unity alternatives, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
