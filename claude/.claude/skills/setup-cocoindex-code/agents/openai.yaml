interface:
  display_name: "Set Up CocoIndex Code"
  short_description: "Configure CocoIndex and local Docling"
  default_prompt: "Use $setup-cocoindex-code after I explicitly provide a non-empty Voyage API key to configure CocoIndex Code and the isolated local Docling document converter."
