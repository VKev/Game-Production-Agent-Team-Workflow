interface:
  display_name: "Unity Flyweight Pattern"
  short_description: "Share Unity data without unsafe state coupling"
  default_prompt: "Use $dev-unity-flyweight-pattern to compare Flyweight with simpler Unity data-sharing and pooling options, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
