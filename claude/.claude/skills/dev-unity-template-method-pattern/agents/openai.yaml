interface:
  display_name: "Unity Template Method Pattern"
  short_description: "Fix algorithm order while varying Unity steps"
  default_prompt: "Use $dev-unity-template-method-pattern to compare its tradeoffs with simpler Unity alternatives, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
