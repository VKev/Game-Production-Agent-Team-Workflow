interface:
  display_name: "Setup Official Unity MCP"
  short_description: "Install official Unity MCP and verify every tool."
  default_prompt: "Use $setup-unity-mcp to configure official Unity MCP for Codex, preserve unrelated AI pages, and prove every registered tool is enabled and visible."
policy:
  allow_implicit_invocation: true
