interface:
  display_name: "Unity Memento Pattern"
  short_description: "Capture and restore Unity state deliberately"
  default_prompt: "Use $dev-unity-memento-pattern to compare its tradeoffs with simpler Unity alternatives, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
