interface:
  display_name: "Unity Observer Pattern"
  short_description: "Build safe Unity events and observable values"
  default_prompt: "Use $dev-unity-observer-pattern to compare and implement the appropriate Unity event, observer, or observable-value approach."
policy:
  allow_implicit_invocation: true
