interface:
  display_name: "Better Context Setup"
  short_description: "Install CLI, Editor bridge, and Unity maps"
  default_prompt: "Use $setup-better-context to install VKev Better Context Unity 1.6 plus its pinned Editor companion package, verify Roslyn and Editor-backed asset/component coverage, configure vendor exclusions, and generate safe project maps."
