interface:
  display_name: "Unity Mediator Pattern"
  short_description: "Coordinate Unity collaborators without a god class"
  default_prompt: "Use $dev-unity-mediator-pattern to compare its tradeoffs with simpler Unity alternatives, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
