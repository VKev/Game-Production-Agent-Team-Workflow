interface:
  display_name: "Unity MCP"
  short_description: "Use official Unity MCP safely and completely."
  default_prompt: "Use $dev-unity-mcp to inspect or change the connected Unity 6.3 project safely."
policy:
  allow_implicit_invocation: true
