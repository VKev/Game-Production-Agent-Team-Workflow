interface:
  display_name: "Unity Interpreter Pattern"
  short_description: "Evaluate small Unity gameplay languages safely"
  default_prompt: "Use $dev-unity-interpreter-pattern to compare its tradeoffs with simpler Unity alternatives, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
