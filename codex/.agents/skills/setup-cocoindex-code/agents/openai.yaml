interface:
  display_name: "Set Up CocoIndex Code"
  short_description: "Configure focused Unity semantic code search"
  default_prompt: "Use $setup-cocoindex-code only after I explicitly provide a non-empty Voyage API key for this setup run."
