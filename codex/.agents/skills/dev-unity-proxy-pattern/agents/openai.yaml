interface:
  display_name: "Unity Proxy Pattern"
  short_description: "Control Unity service access transparently"
  default_prompt: "Use $dev-unity-proxy-pattern to compare its tradeoffs with simpler Unity alternatives, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
