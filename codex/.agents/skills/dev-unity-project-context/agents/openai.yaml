interface:
  display_name: "Unity Project Context"
  short_description: "Route Unity context, retrieval, editing, and maps"
  default_prompt: "Use $dev-unity-project-context to navigate this Unity project, retrieve the relevant source without duplicate searches, route edits safely, and refresh managed maps when needed."
policy:
  allow_implicit_invocation: true
