interface:
  display_name: "Unity Project Context"
  short_description: "Navigate maps with graph and semantic fallback"
  default_prompt: "Use $dev-unity-project-context to treat AGENTS.md as lossy orientation, never infer absence from a map miss, fall back through CodeGraph and CocoIndex, and refresh managed maps after edits."
policy:
  allow_implicit_invocation: true
