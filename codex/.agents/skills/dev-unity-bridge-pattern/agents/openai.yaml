interface:
  display_name: "Unity Bridge Pattern"
  short_description: "Separate Unity abstractions from implementations"
  default_prompt: "Use $dev-unity-bridge-pattern to compare its tradeoffs with simpler Unity alternatives, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
