interface:
  display_name: "Unity Chain of Responsibility"
  short_description: "Build ordered Unity request handling pipelines"
  default_prompt: "Use $dev-unity-chain-of-responsibility-pattern to compare its tradeoffs with simpler Unity alternatives, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
