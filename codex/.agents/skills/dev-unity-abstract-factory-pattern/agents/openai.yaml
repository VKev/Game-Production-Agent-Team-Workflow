interface:
  display_name: "Unity Abstract Factory Pattern"
  short_description: "Create coherent Unity product families safely"
  default_prompt: "Use $dev-unity-abstract-factory-pattern to compare its tradeoffs with simpler Unity alternatives, then design or review the justified solution."
policy:
  allow_implicit_invocation: true
