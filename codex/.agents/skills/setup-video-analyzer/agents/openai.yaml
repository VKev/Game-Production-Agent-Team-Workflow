interface:
  display_name: "Video Analyzer Setup"
  short_description: "Configure caption-first analysis with targeted frames"
  default_prompt: "Use $setup-video-analyzer to configure Codex-only video analysis that reads captions first and retrieves frames only for unresolved visual evidence."
